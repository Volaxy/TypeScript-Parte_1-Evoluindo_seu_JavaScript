// This configures the ts
{
    "compilerOptions": {
        "outDir": "dist/js", // Which folder the JS will be save
        "target": "ES6", // Converts to the version ES6 of JS
        "noEmitOnError": true
    },
    "include": [
        "app/**/*" // Where is the TS files
    ]
}